package `001_basic`

/**
 * @date 2019.05.08
 *
 * fun : Function의 약자로, 함수임을 나타냄
 * - fun을 바로 컴파일하고 실행할 수 있다!
 * - Java처럼 public static void `001_basic`.main 같은거 안 만들어줘도 됨!
 *
 * 코틀린의 특징
 * 1. 패키지 레벨 함수 : 클래스에서 완전히 독립된 함수를 가질 수 있다.
 * 2. 변수의 타입을 맨 끝에 적는다
 * 3. 세미콜론을 적지 않는다.
 */
fun main(args: Array<String>): Unit
{
    println("Hello, Kotlin!")
}