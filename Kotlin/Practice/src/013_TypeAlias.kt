/**
 * @date 2019.05.12
 *
 * typealias : 이미 존재하는 타입에 별명 붙이기!
 * - 그 타입에 새로운 이름을 붙여줄 수 있다
 * - 타입의 이름이 너무 길 때 이름을 줄일 때 유용할 듯
 */

typealias Number = Int
fun main() {
    val a: Number = 10
    println(a)
}